{
  "version": "1.00",
  "_note": "Placeholder only. This project makes NO live third-party API calls (the economic analysis uses published per-token prices), so these limits are unused; they would apply if a live provider were ever wired in.",
  "services": {
    "default": {
      "requests_per_minute": 30,
      "requests_per_hour": 500,
      "concurrent_max": 5,
      "retry_after_seconds": 30,
      "max_retries": 3
    }
  }
}
